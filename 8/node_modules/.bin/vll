#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/bash" ]; then
  "$basedir/bash"  "$basedir/../vl/bin/vll" "$@"
  ret=$?
else 
  bash  "$basedir/../vl/bin/vll" "$@"
  ret=$?
fi
exit $ret
